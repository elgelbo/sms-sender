extends layout.pug

block content
  .container-fluid
    .row.justify-content-md-center.align-items-center
      .col-sm-10.message
        h1 Text 'CEQA' to 
        h1 #{phone}
        p Provide your feedback through a real-time text message poll!
  .container-fluid
      a.float-right#next(href="/results")
        span(data-feather="arrow-right-circle")
