extends layout.pug

include mixins/_adminForm

block nav

block content
  script(src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.1/Chart.min.js")
  .bkd
    .cover
      .container-fluid
        nav.navbar.fixed-top.navbar-light
          a.btn.btn-sm.text-muted(href="/") Home
          a.btn.btn-sm.text-muted(href="/results") Results
      .container-fluid
        .row.justify-content-md-center.align-items-center
          .col-md-10.message
            h1 #{title}
            +adminForm(survey.survey)
